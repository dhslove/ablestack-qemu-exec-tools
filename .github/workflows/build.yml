name: Build and Release ablestack-qemu-exec-tools

on:
  workflow_call:
    inputs:
      source_ref:
        description: "Source commit or ref to build. Empty uses the caller ref."
        required: false
        default: ""
        type: string
      release_tag:
        description: "Release tag used for generated artifact names."
        required: false
        default: ""
        type: string
      publish_release:
        description: "Create the GitHub Release after every build succeeds."
        required: false
        default: false
        type: boolean
      use_prebuilt_winpe:
        description: "Use a WinPE artifact produced earlier in the same workflow run."
        required: false
        default: false
        type: boolean
      winpe_artifact_name:
        description: "WinPE workflow artifact name."
        required: false
        default: "winpe-iso-validation"
        type: string
  workflow_dispatch:
    inputs:
      source_ref:
        description: "Optional source commit or ref. Empty uses the selected workflow ref."
        required: false
        default: ""
        type: string
      release_tag:
        description: "Optional release tag used for generated artifact names. Empty uses an n2k validation tag."
        required: false
        default: ""
        type: string
      publish_release:
        description: "Create or update GitHub Release assets. Keep false for release build validation."
        required: false
        default: false
        type: boolean
      use_prebuilt_winpe:
        description: "Use an existing WinPE artifact from this workflow run. Intended for reusable workflow callers."
        required: false
        default: false
        type: boolean
      winpe_artifact_name:
        description: "WinPE workflow artifact name."
        required: false
        default: "winpe-iso-validation"
        type: string

permissions:
  contents: write
  actions: read

env:
  WORKFLOW_SOURCE_REF: ${{ inputs.source_ref != '' && inputs.source_ref || github.ref }}

# NOTE (WinPE ISO)
# - Manual validation runs build a release-equivalent WinPE ISO in this workflow.
# - Tag-triggered official releases receive the WinPE artifact generated earlier
#   in the same top-level workflow run by build-winpe-release.yml.
# - V2K RPM and release assembly fail closed when the required WinPE ISO is missing.
jobs:
  # Build a release-equivalent WinPE ISO for manual validation runs.
  # The tag-triggered official path reuses the caller-provided WinPE artifact.
  build-winpe:
    if: ${{ !inputs.use_prebuilt_winpe }}
    uses: ./.github/workflows/build-winpe-core.yml
    with:
      source_ref: ${{ inputs.source_ref != '' && inputs.source_ref || github.ref }}
      architecture: "amd64"
      iso_base_name: "winpe-ablestack-v2k"
      iso_suffix: ${{ inputs.release_tag != '' && inputs.release_tag || format('validation-{0}', github.run_number) }}
      artifact_name: ${{ inputs.winpe_artifact_name }}
      retention_days: "14"
      test_mode: false

  # 1) RPM BUILD (Rocky 9.6 / 9.7 / 9.8)
  # ======================================================================
  build-rpm:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        host_os_minor: ["9.6", "9.7", "9.8"]
    container:
      image: rockylinux/rockylinux:9
      options: --user 0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install build deps (Rocky ${{ matrix.host_os_minor }})
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          case "${ROCKY_MINOR}" in
            9.6|9.7) REPO_ROOT="https://dl.rockylinux.org/vault/rocky/${ROCKY_MINOR}" ;;
            9.8) REPO_ROOT="https://dl.rockylinux.org/pub/rocky/${ROCKY_MINOR}" ;;
            *) echo "[ERR] Unsupported Rocky minor for core RPM build: ${ROCKY_MINOR}" >&2; exit 2 ;;
          esac

          cat > "/etc/yum.repos.d/${REPO_PREFIX}.repo" <<EOF
          [${REPO_PREFIX}-baseos]
          name=Rocky Linux ${ROCKY_MINOR} - BaseOS
          baseurl=${REPO_ROOT}/BaseOS/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-appstream]
          name=Rocky Linux ${ROCKY_MINOR} - AppStream
          baseurl=${REPO_ROOT}/AppStream/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-crb]
          name=Rocky Linux ${ROCKY_MINOR} - CRB
          baseurl=${REPO_ROOT}/CRB/\$basearch/os/
          enabled=1
          gpgcheck=0
          EOF

          dnf clean all
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" makecache
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" install make rpm-build tar git dnf-plugins-core createrepo_c

      - name: Build RPM
        run: make rpm

      - name: Create RPM repo (with dependencies) for Rocky ${{ matrix.host_os_minor }}
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          WORKDIR="repo/rpm-rocky${ROCKY_MINOR}"
          mkdir -p "$WORKDIR"
          cp rpmbuild/RPMS/*/*.rpm "$WORKDIR"/

          echo "[INFO] Downloading runtime dependencies..."
          dnf --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" download --resolve --alldeps \
            --destdir="$WORKDIR" \
            bash jq libvirt-client cloud-init || true

          echo "[INFO] Creating repo metadata..."
          createrepo_c "$WORKDIR"

          mkdir -p release/rpm
          mv repo release/rpm/

      - uses: actions/upload-artifact@v4
        with:
          name: rpm-package-${{ matrix.host_os_minor }}
          path: release/rpm/**
  build-hangctl-rpm:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        host_os_minor: ["9.6", "9.7", "9.8"]
    container:
      image: rockylinux/rockylinux:9
      options: --user 0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install build deps (hangctl, Rocky ${{ matrix.host_os_minor }})
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          case "${ROCKY_MINOR}" in
            9.6|9.7) REPO_ROOT="https://dl.rockylinux.org/vault/rocky/${ROCKY_MINOR}" ;;
            9.8) REPO_ROOT="https://dl.rockylinux.org/pub/rocky/${ROCKY_MINOR}" ;;
            *) echo "[ERR] Unsupported Rocky minor for HANGCTL build: ${ROCKY_MINOR}" >&2; exit 2 ;;
          esac

          cat > "/etc/yum.repos.d/${REPO_PREFIX}.repo" <<EOF
          [${REPO_PREFIX}-baseos]
          name=Rocky Linux ${ROCKY_MINOR} - BaseOS
          baseurl=${REPO_ROOT}/BaseOS/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-appstream]
          name=Rocky Linux ${ROCKY_MINOR} - AppStream
          baseurl=${REPO_ROOT}/AppStream/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-crb]
          name=Rocky Linux ${ROCKY_MINOR} - CRB
          baseurl=${REPO_ROOT}/CRB/\$basearch/os/
          enabled=1
          gpgcheck=0
          EOF
          dnf clean all
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" makecache
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" install make rpm-build tar git dnf-plugins-core createrepo_c systemd-rpm-macros systemd

      - name: Build HANGCTL RPM (ablestack_vm_hangctl.spec)
        shell: bash
        run: |
          set -euo pipefail

          SPEC="rpm/ablestack_vm_hangctl.spec"
          test -f "${SPEC}" || (echo "[ERR] Missing spec: ${SPEC}" >&2; exit 2)
          # Read VERSION / RELEASE from the repository VERSION file
          VERSION="$(. ./VERSION; printf "%s" "$VERSION" | tr -d '\r\n[:space:]')"
          RELEASE="$(. ./VERSION; printf "%s" "$RELEASE" | tr -d '\r\n[:space:]')"
          GIT_HASH="$(git rev-parse --short HEAD 2>/dev/null || echo nogit)"

          echo "[INFO] VERSION=${VERSION} RELEASE=${RELEASE} GIT_HASH=${GIT_HASH}"

          mkdir -p rpmbuild_hangctl/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          # Pack sources to a tarball matching the spec Source0 name
          # Pack sources to a tarball that matches the spec Source0 name
          NAME_HANGCTL="ablestack_vm_hangctl"
          TMP_TGZ="$(mktemp /tmp/${NAME_HANGCTL}-${VERSION}.tar.gz.XXXXXX)"
          tar czf "${TMP_TGZ}" \
            --transform="s,^,${NAME_HANGCTL}-${VERSION}/," \
            --exclude=./rpmbuild \
            --exclude=./rpmbuild_v2k \
            --exclude=./rpmbuild_hangctl \
            --exclude=./build \
            --exclude=./release \
            --exclude=./repo \
            --exclude=./dist \
            .
          mv -f "${TMP_TGZ}" "rpmbuild_hangctl/SOURCES/${NAME_HANGCTL}-${VERSION}.tar.gz"

          cp "${SPEC}" rpmbuild_hangctl/SPECS/

          rpmbuild --noplugins -ba --define "_topdir $(pwd)/rpmbuild_hangctl" \
                   --define "version ${VERSION}" \
                   --define "release ${RELEASE}" \
                   --define "githash ${GIT_HASH}" \
                   "rpmbuild_hangctl/SPECS/ablestack_vm_hangctl.spec"

          mkdir -p build/rpm-hangctl
          cp rpmbuild_hangctl/RPMS/noarch/*.rpm build/rpm-hangctl/ 2>/dev/null || true
          cp rpmbuild_hangctl/RPMS/*/*.rpm build/rpm-hangctl/ 2>/dev/null || true

          echo "[INFO] Built RPM(s):"
          ls -lah build/rpm-hangctl || true

      - name: Create HANGCTL RPM repo (Rocky ${{ matrix.host_os_minor }})
        shell: bash
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          WORKDIR="repo/hangctl-rpm-rocky${ROCKY_MINOR}"
          mkdir -p "$WORKDIR"
          # Copy built hangctl RPMs into the local repository
          cp -v build/rpm-hangctl/*.rpm "$WORKDIR"/

          echo "[INFO] Creating repo metadata..."
          createrepo_c "$WORKDIR"

          mkdir -p release/hangctl-rpm
          mv repo release/hangctl-rpm/

      - uses: actions/upload-artifact@v4
        with:
          name: hangctl-rpm-package-rocky${{ matrix.host_os_minor }}
          path: release/hangctl-rpm/**

  # ======================================================================
  # 1-B) FTCTL RPM BUILD (Rocky 9.6 / 9.7 / 9.8)
  # ======================================================================
  build-ftctl-rpm:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        host_os_minor: ["9.6", "9.7", "9.8"]
    container:
      image: rockylinux/rockylinux:9
      options: --user 0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install build deps (ftctl, Rocky ${{ matrix.host_os_minor }})
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          case "${ROCKY_MINOR}" in
            9.6|9.7) REPO_ROOT="https://dl.rockylinux.org/vault/rocky/${ROCKY_MINOR}" ;;
            9.8) REPO_ROOT="https://dl.rockylinux.org/pub/rocky/${ROCKY_MINOR}" ;;
            *) echo "[ERR] Unsupported Rocky minor for FTCTL build: ${ROCKY_MINOR}" >&2; exit 2 ;;
          esac

          cat > "/etc/yum.repos.d/${REPO_PREFIX}.repo" <<EOF
          [${REPO_PREFIX}-baseos]
          name=Rocky Linux ${ROCKY_MINOR} - BaseOS
          baseurl=${REPO_ROOT}/BaseOS/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-appstream]
          name=Rocky Linux ${ROCKY_MINOR} - AppStream
          baseurl=${REPO_ROOT}/AppStream/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-crb]
          name=Rocky Linux ${ROCKY_MINOR} - CRB
          baseurl=${REPO_ROOT}/CRB/\$basearch/os/
          enabled=1
          gpgcheck=0
          EOF
          dnf clean all
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" makecache
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" install make rpm-build tar git dnf-plugins-core createrepo_c systemd-rpm-macros systemd

      - name: Build FTCTL RPM (ablestack_vm_ftctl.spec)
        shell: bash
        run: |
          set -euo pipefail
          make ftctl-rpm

      - name: Create FTCTL RPM repo (Rocky ${{ matrix.host_os_minor }})
        shell: bash
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          WORKDIR="repo/ftctl-rpm-rocky${ROCKY_MINOR}"
          mkdir -p "$WORKDIR"
          cp -v build/rpm-ftctl/*.rpm "$WORKDIR"/
          dnf --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" download --resolve --alldeps \
            --destdir="$WORKDIR" \
            bash jq libvirt-client openssh-clients python3 qemu-img firewalld nmap-ncat socat util-linux || true
          createrepo_c "$WORKDIR"
          mkdir -p release/ftctl-rpm
          mv repo release/ftctl-rpm/

      - uses: actions/upload-artifact@v4
        with:
          name: ftctl-rpm-package-rocky${{ matrix.host_os_minor }}
          path: release/ftctl-rpm/**

          
  # 1-C) V2K RPM BUILD (Rocky 9.6 / 9.7 / 9.8)
  # 1-C) V2K RPM BUILD (isolated path to avoid overlap with other RPM jobs)
  # ======================================================================
  build-v2k-rpm:
    needs: [build-winpe]
    if: ${{ always() && (inputs.use_prebuilt_winpe || needs.build-winpe.result == 'success') }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        host_os_minor: ["9.6", "9.7", "9.8"]
    container:
      image: rockylinux/rockylinux:9
      options: --user 0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install build deps (v2k, Rocky ${{ matrix.host_os_minor }})
        run: |
          set -euo pipefail

          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          case "${ROCKY_MINOR}" in
            9.6|9.7) REPO_ROOT="https://dl.rockylinux.org/vault/rocky/${ROCKY_MINOR}" ;;
            9.8) REPO_ROOT="https://dl.rockylinux.org/pub/rocky/${ROCKY_MINOR}" ;;
            *)
              echo "[ERR] Unsupported Rocky minor for V2K build: ${ROCKY_MINOR}" >&2
              exit 2
              ;;
          esac

          # Keep V2K build inputs pinned to the intended Rocky minor.
          # 9.6/9.7 are archived in vault; 9.8 is the current published minor.
          cat > "/etc/yum.repos.d/${REPO_PREFIX}.repo" <<EOF
          [${REPO_PREFIX}-baseos]
          name=Rocky Linux ${ROCKY_MINOR} - BaseOS
          baseurl=${REPO_ROOT}/BaseOS/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-appstream]
          name=Rocky Linux ${ROCKY_MINOR} - AppStream
          baseurl=${REPO_ROOT}/AppStream/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-crb]
          name=Rocky Linux ${ROCKY_MINOR} - CRB
          baseurl=${REPO_ROOT}/CRB/\$basearch/os/
          enabled=1
          gpgcheck=0
          EOF

          dnf clean all
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" makecache
          # Handle curl vs curl-minimal replacement in the Rocky container image
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" install \
            --allowerasing ca-certificates curl-minimal || true

          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" install \
            make rpm-build tar git jq dnf-plugins-core createrepo_c

      - name: Configure Rocky ${{ matrix.host_os_minor }} build repos + enable EPEL (build-time only)
        run: |
          set -euo pipefail

          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          case "${ROCKY_MINOR}" in
            9.6|9.7) REPO_ROOT="https://dl.rockylinux.org/vault/rocky/${ROCKY_MINOR}" ;;
            9.8) REPO_ROOT="https://dl.rockylinux.org/pub/rocky/${ROCKY_MINOR}" ;;
            *)
              echo "[ERR] Unsupported Rocky minor for V2K build: ${ROCKY_MINOR}" >&2
              exit 2
              ;;
          esac

          cat > "/etc/yum.repos.d/${REPO_PREFIX}.repo" <<EOF
          [${REPO_PREFIX}-baseos]
          name=Rocky Linux ${ROCKY_MINOR} - BaseOS
          baseurl=${REPO_ROOT}/BaseOS/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-appstream]
          name=Rocky Linux ${ROCKY_MINOR} - AppStream
          baseurl=${REPO_ROOT}/AppStream/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-crb]
          name=Rocky Linux ${ROCKY_MINOR} - CRB
          baseurl=${REPO_ROOT}/CRB/\$basearch/os/
          enabled=1
          gpgcheck=0
          EOF

          echo "[INFO] Enabled repos (${ROCKY_MINOR}):"
          grep -nE '^\[|^baseurl=' "/etc/yum.repos.d/${REPO_PREFIX}.repo"

          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" makecache
          # Enable EPEL only for build-time dependencies such as nbd-related packages
          # Install epel-release directly because it may not resolve from vault-only repos.
          # Detect the active EPEL repo id after installation.
          # Use that detected repo id for later repoquery/download steps.
          # Use the detected repo id for later repoquery and download steps.
          EPEL_RPM_URL="https://dl.fedoraproject.org/pub/epel/epel-release-latest-9.noarch.rpm"
          echo "[INFO] Installing EPEL release RPM: ${EPEL_RPM_URL}"
          curl -fL -o /tmp/epel-release.rpm "${EPEL_RPM_URL}"
          rpm -Uvh --nosignature /tmp/epel-release.rpm

          # Detect the effective EPEL repository id after epel-release installation
          EPEL_REPO_ID="$(dnf repolist all 2>/dev/null | awk 'tolower($1) ~ /^epel/ {print $1; exit}')"
          if [[ -z "${EPEL_REPO_ID}" ]]; then
            echo "[ERR] EPEL repo id not found after installing epel-release." >&2
            dnf repolist all || true
            exit 2
          fi
          echo "[INFO] Detected EPEL repo id: ${EPEL_REPO_ID}"
          echo "EPEL_REPO_ID=${EPEL_REPO_ID}" >> "$GITHUB_ENV"

          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*","${EPEL_REPO_ID}" makecache

      - name: Stage generated WinPE ISO into V2K RPM source
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.winpe_artifact_name }}
          path: winpe

      - name: Show staged WinPE ISO for V2K RPM
        shell: bash
        run: |
          set -euo pipefail
          echo "[INFO] WinPE files staged before V2K RPM build:"
          find winpe -maxdepth 1 -type f -printf '%f %s bytes\n' | sort || true
          mapfile -d '' -t WINPE_ISOS < <(
            find winpe -maxdepth 1 -type f -iname '*.iso' -print0 | sort -z
          )
          if [[ "${#WINPE_ISOS[@]}" -ne 1 ]]; then
            echo "[ERR] Expected exactly one staged WinPE ISO, found ${#WINPE_ISOS[@]}." >&2
            exit 2
          fi
          (
            cd winpe
            sha256sum -c SHA256SUMS
          )
          RELEASE_TAG="${{ inputs.release_tag }}"
          WINPE_SUFFIX="${RELEASE_TAG:-validation-${GITHUB_RUN_NUMBER}}"
          EXPECTED_NAME="winpe-ablestack-v2k-${WINPE_SUFFIX}-amd64.iso"
          if [[ "$(basename "${WINPE_ISOS[0]}")" != "${EXPECTED_NAME}" ]]; then
            echo "[ERR] Versioned WinPE filename mismatch: expected=${EXPECTED_NAME} actual=$(basename "${WINPE_ISOS[0]}")" >&2
            exit 2
          fi

      - name: Verify V2K Cloud service-offering CPU speed handling
        shell: bash
        run: bash tests/v2k_cloud_offering_cpu_speed_smoke.sh

      - name: Build V2K RPM (separate path)
        run: |
          set -e
          # Build V2K RPM in an isolated path to avoid overlap with other packaging jobs
          make v2k-rpm
 
      - name: Verify ablestack_v2k rpm contains bash completion and runtime assets
        shell: bash
        run: |
          set -euo pipefail
          RPM_FILE=$(ls -1 build/rpm-v2k/ablestack_v2k-*.rpm | head -n 1)
          echo "[INFO] verifying rpm: ${RPM_FILE}"
          rpm -qlp "${RPM_FILE}" | grep -E "bash-completion/completions/ablestack_v2k$" >/dev/null
          rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/runtime-assets/assets/govc_Linux_x86_64.tar.gz$" >/dev/null
          rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/runtime-assets/assets/VMware-vix-disklib-.*[.]tar[.]gz$" >/dev/null
          rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/compat/esxi55/profile.json$" >/dev/null
          rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/runtime-assets/assets/compat/esxi55/VMware-vix-disklib-6[.]5[.]0-4604867[.]x86_64[.]tar[.]gz$" >/dev/null
          rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/runtime-assets/assets/compat/esxi55/nbdkit-vddk-legacy-1[.]14[.]2-rocky9-x86_64[.]tar[.]gz$" >/dev/null
          rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/runtime-assets/assets/compat/esxi55/wheels/pyvmomi-5[.]5[.]0[.]2014[.]1[.]1[.]tar[.]gz$" >/dev/null
          if find winpe -maxdepth 1 -type f -iname '*.iso' | grep -q .; then
            rpm -qlp "${RPM_FILE}" | grep -E "/usr/share/ablestack/v2k/winpe/.*[.]iso$" >/dev/null
          fi
          echo "[OK] bash completion and runtime assets are packaged"

      - name: Create V2K RPM repo (offline) for ABLESTACK (Rocky ${{ matrix.host_os_minor }})
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-build-${ROCKY_MINOR}"
          WORKDIR="release/v2k/v2k-rpm-rocky${ROCKY_MINOR}"
          mkdir -p "$WORKDIR"

          # Collect only installable V2K binary RPMs into the final offline repo.
          # Source RPMs carry the full bundled asset tree and can push the Rocky
          # release ISO over GitHub's 2 GiB single-asset limit.
          cp -v build/rpm-v2k/ablestack_v2k-*.noarch.rpm "$WORKDIR"/

          echo "[INFO] Collecting runtime deps for Air-Gap (Requires closure minus ABLESTACK baseline)..."

          : "${EPEL_REPO_ID:?EPEL_REPO_ID is not set. Configure step must export it to GITHUB_ENV}"
          DNF_COMMON_OPTS=(
            --releasever=${ROCKY_MINOR}
            --disablerepo=*
            --enablerepo="${REPO_PREFIX}-*","${EPEL_REPO_ID}"
            --setopt=install_weak_deps=False
          )

          REQ_PKGS=(
            jq
            python3
            python3-pip
            openssl
            nbd
            nbdkit
            nbdkit-vddk-plugin
            qemu-img
            libvirt-client
            tar
          )

          BASELINE_FILE="rpm/v2k_baseline_pkgs_ablestack_${ROCKY_MINOR}.txt"
          COMPAT_BASELINE_FILE="rpm/v2k_baseline_pkgs_ablestack_9.7.txt"
          SHARED_BASELINE_FILE="rpm/v2k_baseline_pkgs_ablestack_9.6.txt"

          echo "[INFO] Sanity check: required pkgs availability (repo view)"
          dnf -q "${DNF_COMMON_OPTS[@]}" repoquery --available "${REQ_PKGS[@]}" >/dev/null

          dnf -y "${DNF_COMMON_OPTS[@]}" install dnf-plugins-core >/dev/null
          dnf -q "${DNF_COMMON_OPTS[@]}" repoquery \
            --requires --resolve --recursive \
            --qf '%{name}' \
            "${REQ_PKGS[@]}" | sed '/^$/d' | sort -u > /tmp/v2k_dep_closure.txt

          TMP_PREFIX="/tmp/v2k-${ROCKY_MINOR}"
          printf '%s\n' "${REQ_PKGS[@]}" | sed '/^$/d' | sort -u > "${TMP_PREFIX}-must-have.txt"

          EFFECTIVE_BASELINE_FILE="${BASELINE_FILE}"
          if [[ -f "${BASELINE_FILE}" ]]; then
            echo "[INFO] Using ABLESTACK baseline file: ${BASELINE_FILE}"
          elif [[ "${ROCKY_MINOR}" == "9.8" && -f "${COMPAT_BASELINE_FILE}" ]]; then
            EFFECTIVE_BASELINE_FILE="${COMPAT_BASELINE_FILE}"
            echo "[INFO] Using Rocky 9.7 ABLESTACK baseline for Rocky 9.8 compatibility: ${COMPAT_BASELINE_FILE}"
          elif [[ -f "${SHARED_BASELINE_FILE}" ]]; then
            EFFECTIVE_BASELINE_FILE="${SHARED_BASELINE_FILE}"
            echo "[WARN] Missing minor-specific baseline: ${BASELINE_FILE}"
            echo "[INFO] Reusing shared ABLESTACK 9.6 baseline: ${SHARED_BASELINE_FILE}"
          else
            echo "[ERR] Missing baseline file: ${BASELINE_FILE}" >&2
            echo "[ERR] Missing shared fallback baseline: ${SHARED_BASELINE_FILE}" >&2
            echo "      Provide at least ${SHARED_BASELINE_FILE} to avoid downloading unnecessary host packages." >&2
            exit 2
          fi

          sort -u "${EFFECTIVE_BASELINE_FILE}" > "${TMP_PREFIX}-baseline.txt"
          comm -23 /tmp/v2k_dep_closure.txt "${TMP_PREFIX}-baseline.txt" > "${TMP_PREFIX}-to-download-closure.txt"

          # must-have + (closure-baseline) = candidate list
          cat "${TMP_PREFIX}-must-have.txt" "${TMP_PREFIX}-to-download-closure.txt" | sort -u > "${TMP_PREFIX}-to-download-candidate.txt"

          echo "[INFO] Candidate(to-download) count: $(wc -l < "${TMP_PREFIX}-to-download-candidate.txt")"
          echo "[INFO] Candidate sample (first 50):"
          head -n 50 "${TMP_PREFIX}-to-download-candidate.txt" || true

          echo "[INFO] Filtering candidate list by repo availability (STRICT)..."
          : > "${TMP_PREFIX}-to-download-avail.txt"
          cat "${TMP_PREFIX}-to-download-candidate.txt" \
            | xargs -r -n 200 dnf -q "${DNF_COMMON_OPTS[@]}" repoquery --available --qf '%{name}' \
            | sort -u > "${TMP_PREFIX}-to-download-avail.txt"

          grep -vE '^glibc-all-langpacks$|^glibc-langpack-' "${TMP_PREFIX}-to-download-avail.txt" \
            | sort -u > "${TMP_PREFIX}-to-download.txt"

          comm -23 "${TMP_PREFIX}-to-download-candidate.txt" "${TMP_PREFIX}-to-download-avail.txt" > "${TMP_PREFIX}-missing-in-repo.txt"
          if [[ -s "${TMP_PREFIX}-missing-in-repo.txt" ]]; then
            echo "[WARN] Missing in enabled repos (skipped):"
            head -n 50 "${TMP_PREFIX}-missing-in-repo.txt" || true
          fi

          echo "[INFO] Final to-download count(after avail+langpack filter): $(wc -l < "${TMP_PREFIX}-to-download.txt")"
          echo "[INFO] Final to-download sample (first 50):"
          head -n 50 "${TMP_PREFIX}-to-download.txt" || true

          for p in nbd nbdkit qemu-img; do
            if ! grep -qx "$p" "${TMP_PREFIX}-to-download.txt" && ! grep -qx "$p" "${TMP_PREFIX}-baseline.txt"; then
              echo "[ERR] Required package '$p' is neither in baseline nor in final download list." >&2
              exit 2
            fi
          done
          if [[ -s "${TMP_PREFIX}-to-download.txt" ]]; then
            xargs -r dnf "${DNF_COMMON_OPTS[@]}" download --destdir="$WORKDIR" < "${TMP_PREFIX}-to-download.txt"
          else
            echo "[INFO] Nothing to download (all deps satisfied by baseline)."
          fi

          echo "[INFO] Creating repo metadata (createrepo_c)..."
          createrepo_c "$WORKDIR"

          echo "[INFO] Repo metadata sanity check:"
          test -f "$WORKDIR/repodata/repomd.xml"
          ls -al "$WORKDIR/repodata" | head -n 50 || true

      - uses: actions/upload-artifact@v4
        with:
          name: v2k-rpm-package-rocky${{ matrix.host_os_minor }}
          path: release/v2k/**

  # ======================================================================
  # 1-D) N2K RPM BUILD (Rocky 9.6 / 9.7 / 9.8)
  # ======================================================================
  build-n2k-rpm:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        host_os_minor: ["9.6", "9.7", "9.8"]
    container:
      image: rockylinux/rockylinux:9
      options: --user 0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install build deps (n2k, Rocky ${{ matrix.host_os_minor }})
        shell: bash
        run: |
          set -euo pipefail

          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-n2k-${ROCKY_MINOR}"
          case "${ROCKY_MINOR}" in
            9.6|9.7) REPO_ROOT="https://dl.rockylinux.org/vault/rocky/${ROCKY_MINOR}" ;;
            9.8) REPO_ROOT="https://dl.rockylinux.org/pub/rocky/${ROCKY_MINOR}" ;;
            *)
              echo "[ERR] Unsupported Rocky minor for N2K build: ${ROCKY_MINOR}" >&2
              exit 2
              ;;
          esac

          cat > "/etc/yum.repos.d/${REPO_PREFIX}.repo" <<EOF
          [${REPO_PREFIX}-baseos]
          name=Rocky Linux ${ROCKY_MINOR} - BaseOS
          baseurl=${REPO_ROOT}/BaseOS/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-appstream]
          name=Rocky Linux ${ROCKY_MINOR} - AppStream
          baseurl=${REPO_ROOT}/AppStream/\$basearch/os/
          enabled=1
          gpgcheck=0

          [${REPO_PREFIX}-crb]
          name=Rocky Linux ${ROCKY_MINOR} - CRB
          baseurl=${REPO_ROOT}/CRB/\$basearch/os/
          enabled=1
          gpgcheck=0
          EOF

          dnf clean all
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" makecache
          dnf -y --disablerepo="*" --enablerepo="${REPO_PREFIX}-*" install \
            make rpm-build tar git dnf-plugins-core createrepo_c

      - name: Build N2K RPM
        run: make n2k-rpm

      - name: Create N2K RPM repo (Rocky ${{ matrix.host_os_minor }})
        shell: bash
        run: |
          set -euo pipefail
          ROCKY_MINOR="${{ matrix.host_os_minor }}"
          REPO_PREFIX="rocky-n2k-${ROCKY_MINOR}"
          WORKDIR="release/n2k/n2k-rpm-rocky${ROCKY_MINOR}"
          mkdir -p "${WORKDIR}"

          cp -v build/rpm-n2k/*.rpm "${WORKDIR}/"

          echo "[INFO] Downloading N2K runtime dependencies for Rocky ${ROCKY_MINOR}..."
          dnf --releasever="${ROCKY_MINOR}" \
            --disablerepo="*" \
            --enablerepo="${REPO_PREFIX}-*" \
            --setopt=install_weak_deps=False \
            download --resolve --alldeps \
            --destdir="${WORKDIR}" \
            bash bash-completion jq curl openssl qemu-img libvirt-client nfs-utils || true

          createrepo_c "${WORKDIR}"
          test -f "${WORKDIR}/repodata/repomd.xml"

      - uses: actions/upload-artifact@v4
        with:
          name: n2k-rpm-package-rocky${{ matrix.host_os_minor }}
          path: release/n2k/**

  # ======================================================================
  # 2) DEB BUILD (Ubuntu 22.04 / 24.04 / 26.04)
  # ======================================================================
  build-deb:
    strategy:
      matrix:
        os_version: [22.04, 24.04, 26.04]
    runs-on: ubuntu-${{ matrix.os_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install build deps
        run: |
          sudo apt-get update -y
          sudo apt-get install -y build-essential make dpkg-dev debhelper git apt-utils

      - name: Build DEB
        run: make deb

      - name: Download dependencies and create APT repo
        run: |
          set -e
          sudo apt-get update -y

          mkdir -p repo/pool
          find build/deb -type f -name "*.deb" -exec cp {} repo/pool/ \;

          cd repo/pool

          echo "[INFO] Starting dependency collection..."
          seen_pkgs=""
          new_pkgs=1
          while [ "$new_pkgs" -eq 1 ]; do
            new_pkgs=0
            for deb in *.deb; do
              [ -e "$deb" ] || continue
              P=$(dpkg-deb -f "$deb" Package)
              if echo "$seen_pkgs" | grep -qw "$P"; then
                continue
              fi
              seen_pkgs="$seen_pkgs $P"

              DEPS=$(dpkg-deb -f "$deb" Depends | \
                     tr ',' '\n'          | \
                     sed -E 's/\(.*\)//g' | \
                     sed 's/|/ /g'        | \
                     sed 's/:any//g'      | \
                     awk '{print $1}'     | \
                     grep -v '^$'         | \
                     sort -u)
              echo "[INFO] [$P] depends on: $DEPS"

              for dep in $DEPS; do
                [ -n "$dep" ] || continue
                # Download the dependency only if it is not already present in repo/pool
                if ls -1 ${dep}_*.deb >/dev/null 2>&1; then
                  continue
                fi
                echo "[INFO] Downloading: $dep"
                if apt-get download "$dep"; then
                  new_pkgs=1
                else
                  echo "[WARN] Failed to download: $dep"
                fi
              done
            done
          done

          echo "[INFO] Total pool count: $(ls -1 *.deb | wc -l)"

          cd ../..
          # Generate repository metadata using repo/pool as the package source
          mkdir -p repo/dists/stable/main/binary-amd64
          ( cd repo && dpkg-scanpackages pool /dev/null | gzip -9c > dists/stable/main/binary-amd64/Packages.gz )

          # Build the final DEB repository structure
          mv repo "deb-ubuntu${{ matrix.os_version }}"
          mkdir -p release/deb
          mv deb-ubuntu${{ matrix.os_version }} release/deb/

      - uses: actions/upload-artifact@v4
        with:
          name: deb-package-${{ matrix.os_version }}
          path: release/deb/**

  # ======================================================================
  # 3) WINDOWS MSI BUILD
  # ======================================================================
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Install WiX v4
        run: dotnet tool install --global wix --version 4.*

      - name: Add dotnet tools to PATH
        shell: pwsh
        run: echo "$env:USERPROFILE\.dotnet\tools" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Build MSI
        shell: pwsh
        run: make windows

      - uses: actions/upload-artifact@v4
        with:
          name: msi-package
          path: windows/msi/out/*.msi
          if-no-files-found: error

  # ======================================================================
  # 4) RELEASE + ISO CREATION (VirtIO ???? ????
  # ======================================================================
  release:
    needs: [build-rpm, build-hangctl-rpm, build-ftctl-rpm, build-deb, build-windows, build-v2k-rpm, build-n2k-rpm]
    runs-on: ubuntu-latest
    # The official path deliberately skips this reusable workflow's build-winpe
    # job because the caller already produced it. Use always() to prevent that
    # skipped ancestor from suppressing release, while still requiring every
    # direct build dependency to succeed.
    if: >-
      ${{
        always() &&
        needs['build-rpm'].result == 'success' &&
        needs['build-hangctl-rpm'].result == 'success' &&
        needs['build-ftctl-rpm'].result == 'success' &&
        needs['build-deb'].result == 'success' &&
        needs['build-windows'].result == 'success' &&
        needs['build-v2k-rpm'].result == 'success' &&
        needs['build-n2k-rpm'].result == 'success'
      }}
    steps:
      - name: Checkout release source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.WORKFLOW_SOURCE_REF }}

      - name: Resolve release context
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ inputs.release_tag }}"
          PUBLISH="${{ inputs.publish_release }}"

          if [[ -z "$TAG" ]]; then
            TAG="n2k-validation-${GITHUB_RUN_NUMBER}-${GITHUB_SHA::7}"
          fi
          if [[ "${PUBLISH}" == "true" && "$TAG" != v* ]]; then
            echo "[ERR] publish_release=true requires a v* release tag: $TAG" >&2
            exit 2
          fi

          REF="$(git rev-parse HEAD)"
          echo "RELEASE_TAG=$TAG" >> "$GITHUB_ENV"
          echo "RELEASE_REF=$REF" >> "$GITHUB_ENV"
          echo "RELEASE_PUBLISH=$PUBLISH" >> "$GITHUB_ENV"
          echo "[INFO] RELEASE_TAG=$TAG"
          echo "[INFO] RELEASE_REF=$REF"
          echo "[INFO] RELEASE_PUBLISH=$PUBLISH"

      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Install packaging tools (genisoimage, 7zip)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y genisoimage p7zip-full python3-pip

      - name: Collect artifacts into structured dirs
        run: |
          set -e
          mkdir -p release/rpm release/deb release/msi release/assets/v2k/wheels release/v2k release/completions release/bin

          echo "[INFO] Collecting RPM repos..."
          for ver in 9.6 9.7 9.8; do
            SRC_DIR=$(find dist -type d -path "*/rpm-package-${ver}/release/rpm/repo/rpm-rocky${ver}" | head -n 1)
            if [ -d "$SRC_DIR" ]; then
              echo "[INFO] Found $SRC_DIR"
              mkdir -p release/rpm/rpm-rocky${ver}
              cp -r "$SRC_DIR"/* release/rpm/rpm-rocky${ver}/
            else
              SRC_DIR=$(find dist -type d -path "*/rpm-package-${ver}/repo/rpm-rocky${ver}" | head -n 1)
              if [ -d "$SRC_DIR" ]; then
                echo "[INFO] Found (fallback) $SRC_DIR"
                mkdir -p release/rpm/rpm-rocky${ver}
                cp -r "$SRC_DIR"/* release/rpm/rpm-rocky${ver}/
              else
                echo "[ERR] No core RPM repo found for Rocky ${ver}." >&2
                exit 2
              fi
            fi
          done

          for ver in 9.6 9.7 9.8; do
            echo "[INFO] Collecting HANGCTL RPM repo (Rocky ${ver})..."
            HANGCTL_DIR="$(find dist -type d \( \
                            -path "*/hangctl-rpm-package-rocky${ver}/release/hangctl-rpm/repo/hangctl-rpm-rocky${ver}" -o \
                            -path "*/hangctl-rpm-package-rocky${ver}/repo/hangctl-rpm-rocky${ver}" \
                          \) | head -n 1 || true)"
            if [[ -z "${HANGCTL_DIR}" || ! -d "${HANGCTL_DIR}" ]]; then
              echo "[ERR] No HANGCTL RPM repo found for Rocky ${ver}." >&2
              exit 2
            fi
            mkdir -p "release/rpm/hangctl-rpm-rocky${ver}"
            cp -r "${HANGCTL_DIR}/." "release/rpm/hangctl-rpm-rocky${ver}/"

            echo "[INFO] Collecting FTCTL RPM repo (Rocky ${ver})..."
            FTCTL_DIR="$(find dist -type d \( \
                            -path "*/ftctl-rpm-package-rocky${ver}/release/ftctl-rpm/repo/ftctl-rpm-rocky${ver}" -o \
                            -path "*/ftctl-rpm-package-rocky${ver}/repo/ftctl-rpm-rocky${ver}" \
                          \) | head -n 1 || true)"
            if [[ -z "${FTCTL_DIR}" || ! -d "${FTCTL_DIR}" ]]; then
              echo "[ERR] No FTCTL RPM repo found for Rocky ${ver}." >&2
              exit 2
            fi
            mkdir -p "release/rpm/ftctl-rpm-rocky${ver}"
            cp -r "${FTCTL_DIR}/." "release/rpm/ftctl-rpm-rocky${ver}/"
          done

          echo "[INFO] Staging bash completion files..."
          if [[ -f completions/ablestack_v2k ]]; then
            cp -f completions/ablestack_v2k release/completions/
          fi
          if [[ -f completions/ablestack_vm_ftctl ]]; then
            cp -f completions/ablestack_vm_ftctl release/completions/
          fi
          if [[ -f completions/ablestack_n2k ]]; then
            cp -f completions/ablestack_n2k release/completions/
          fi

          echo "[INFO] Collecting DEB repos..."
          for ver in 22.04 24.04 26.04; do
            SRC_DIR=$(find dist -type d \( \
                        -path "*/deb-package-${ver}/release/deb/deb-ubuntu${ver}" -o \
                        -path "*/deb-package-${ver}/deb-ubuntu${ver}" \
                      \) | head -n 1)
            if [ -d "$SRC_DIR" ]; then
              echo "[INFO] Found $SRC_DIR"
              mkdir -p release/deb/deb-ubuntu${ver}
              cp -r "$SRC_DIR"/* release/deb/deb-ubuntu${ver}/
            else
              echo "[ERR] No DEB repo found for Ubuntu ${ver} in workflow artifacts." >&2
              find dist -maxdepth 6 -path "*deb-package*" -print >&2 || true
              exit 2
            fi
          done

          echo "[INFO] Collecting MSI artifacts..."
          MSI_FILE="$(find dist -type f -path "*/msi-package/*.msi" -print -quit)"
          if [[ -z "${MSI_FILE}" ]]; then
            echo "[ERR] No MSI artifacts found in the msi-package workflow artifact." >&2
            exit 2
          fi
          find dist -type f -path "*/msi-package/*.msi" -exec cp {} release/msi/ \;
          test -n "$(find release/msi -maxdepth 1 -type f -name '*.msi' -print -quit)"

          echo "[INFO] Staging helper scripts..."
          if [[ -f bin/v2k_test_install.sh ]]; then
            cp -f bin/v2k_test_install.sh release/bin/
          fi

      - name: Collect V2K RPM repos into ISO tree
        shell: bash
        run: |
          set -euo pipefail
          for ver in 9.6 9.7 9.8; do
            SRC_DIR=$(find dist -type d -path "*/v2k-rpm-rocky${ver}" | head -n 1 || true)
            if [[ -z "${SRC_DIR}" || ! -d "${SRC_DIR}" ]]; then
              echo "[ERR] No V2K RPM repo found for Rocky ${ver} in workflow artifacts." >&2
              echo "[ERR] Debug: listing dist/*v2k-rpm* tree (maxdepth=6)" >&2
              find dist -maxdepth 6 -path "*v2k-rpm*" -print >&2 || true
              exit 2
            fi

            echo "[INFO] Found V2K repo for Rocky ${ver}: ${SRC_DIR}"
            mkdir -p "release/v2k/v2k-rpm-rocky${ver}"
            cp -a "${SRC_DIR}/." "release/v2k/v2k-rpm-rocky${ver}/"
          done

      - name: Collect N2K repos into ISO tree
        shell: bash
        run: |
          set -euo pipefail

          for ver in 9.6 9.7 9.8; do
            SRC_DIR="$(find dist -type d \( \
                        -path "*/n2k-rpm-package-rocky${ver}/release/n2k/n2k-rpm-rocky${ver}" -o \
                        -path "*/n2k-rpm-package-rocky${ver}/n2k-rpm-rocky${ver}" \
                      \) | head -n 1 || true)"
            if [[ -z "${SRC_DIR}" || ! -d "${SRC_DIR}" ]]; then
              echo "[ERR] No N2K RPM repo found for Rocky ${ver} in workflow artifacts." >&2
              find dist -maxdepth 6 -path "*n2k-rpm*" -print >&2 || true
              exit 2
            fi
            echo "[INFO] Found N2K RPM repo for Rocky ${ver}: ${SRC_DIR}"
            mkdir -p "release/n2k/n2k-rpm-rocky${ver}"
            cp -a "${SRC_DIR}/." "release/n2k/n2k-rpm-rocky${ver}/"
          done

      - name: Stage V2K assets from repo ./assets into ISO (govc, VDDK)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release/assets release/assets/compat release/assets/v2k release/share/ablestack/v2k
          cp -v assets/govc_Linux_x86_64.tar.gz release/assets/ || true
          cp -v assets/VMware-vix-disklib-*.tar.gz release/assets/ || true
          if [[ -d assets/compat ]]; then
            cp -a assets/compat/. release/assets/compat/
          fi
          if [[ -d share/ablestack/v2k/compat ]]; then
            cp -a share/ablestack/v2k/compat release/share/ablestack/v2k/
          fi

      - name: Prepare pyvmomi offline wheels (Air-Gap)
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip download -d release/assets/v2k/wheels "pyvmomi==8.0.3.0.1"

      # VirtIO Windows payload (amd64 drivers and guest tools)
      # Extract only the payloads required by the Windows installer and WinPE flow.
      - name: Download & extract VirtIO (amd64-only + guest tools)
        run: |
          set -e
          mkdir -p dist/virtio-src release/virtio
          curl -fL -o dist/virtio-win.iso \
            "https://fedorapeople.org/groups/virt/virtio-win/direct-downloads/stable-virtio/virtio-win.iso"

          # Extract amd64 driver payloads used by DISM/WinPE
          7z x -y -o"dist/virtio-src" dist/virtio-win.iso \
            "viostor/*/amd64/*" \
            "vioscsi/*/amd64/*" \
            "NetKVM/*/amd64/*" \
            "Balloon/*/amd64/*" \
            "qemupciserial/*/amd64/*" \
            "viorng/*/amd64/*" \
            "pvpanic/*/amd64/*" \
            "vioinput/*/amd64/*" \
            "vioser/*/amd64/*" || true

          # Extract guest tools installers (exe/msi)
          7z x -y -o"dist/virtio-src" dist/virtio-win.iso "virtio-win-guest-tools.exe" || true
          7z x -y -o"dist/virtio-src" dist/virtio-win.iso "virtio-win-gt-*.msi" || true

          # Stage extracted driver trees into release/virtio
          for d in viostor vioscsi NetKVM Balloon qemupciserial viorng pvpanic vioinput vioser; do
            if [[ -d "dist/virtio-src/${d}" ]]; then
              mkdir -p "release/virtio/${d}"
              cp -a "dist/virtio-src/${d}/." "release/virtio/${d}/"
            fi
          done
          if [[ -f "dist/virtio-src/virtio-win-guest-tools.exe" ]]; then
            cp -a "dist/virtio-src/virtio-win-guest-tools.exe" "release/virtio/"
          fi
          shopt -s nullglob
          for m in dist/virtio-src/virtio-win-gt-*.msi; do
            cp -a "$m" "release/virtio/"
          done
          shopt -u nullglob

          echo "[INFO] virtio payload size:"
          du -sh release/virtio || true
          echo "[INFO] sample INF files:"
          find release/virtio -type f -name '*.inf' | head -n 30 || true

      - name: Stage required WinPE ISO into release tree
        shell: bash
        run: |
          set -euo pipefail
          WINPE_ARTIFACT_DIR="dist/${{ inputs.winpe_artifact_name }}"
          if [[ ! -d "${WINPE_ARTIFACT_DIR}" ]]; then
            echo "[ERR] Required WinPE artifact directory was not found: ${WINPE_ARTIFACT_DIR}" >&2
            exit 2
          fi
          mapfile -d '' -t WINPE_ISOS < <(
            find "${WINPE_ARTIFACT_DIR}" -maxdepth 1 -type f -iname '*.iso' -print0 | sort -z
          )
          if [[ "${#WINPE_ISOS[@]}" -ne 1 ]]; then
            echo "[ERR] Expected exactly one WinPE ISO in ${WINPE_ARTIFACT_DIR}, found ${#WINPE_ISOS[@]}." >&2
            exit 2
          fi
          WINPE_SUM="${WINPE_ARTIFACT_DIR}/SHA256SUMS"
          if [[ ! -f "${WINPE_SUM}" ]]; then
            echo "[ERR] SHA256SUMS was not found beside the WinPE ISO." >&2
            exit 2
          fi
          (
            cd "${WINPE_ARTIFACT_DIR}"
            sha256sum -c SHA256SUMS
          )
          WINPE_SUFFIX="${{ inputs.release_tag }}"
          WINPE_SUFFIX="${WINPE_SUFFIX:-validation-${GITHUB_RUN_NUMBER}}"
          EXPECTED_NAME="winpe-ablestack-v2k-${WINPE_SUFFIX}-amd64.iso"
          if [[ "$(basename "${WINPE_ISOS[0]}")" != "${EXPECTED_NAME}" ]]; then
            echo "[ERR] Release WinPE filename mismatch: expected=${EXPECTED_NAME} actual=$(basename "${WINPE_ISOS[0]}")" >&2
            exit 2
          fi
          rm -rf release/winpe
          mkdir -p release/winpe
          cp -a "${WINPE_ISOS[0]}" release/winpe/
          cp -a "${WINPE_SUM}" release/winpe/SHA256SUMS
          (
            cd release/winpe
            sha256sum -c SHA256SUMS
          )
          echo "[OK] Staged versioned WinPE ISO:"
          ls -lah release/winpe/*.iso release/winpe/SHA256SUMS

      # Windows installer script generation (install.ps1 / install.bat)
      - name: Create Windows installer scripts
        shell: bash
        run: |
          cat > release/install.ps1 << 'PS1'
          Param(
            [switch]$Silent
          )

          # Force default = false if not provided
          if (-not $PSBoundParameters.ContainsKey('Silent')) {
            $Silent = $false
          }

          # Debug print
          Write-Host "[DEBUG] Silent mode: $Silent"

          function Ensure-Admin {
            $id = [Security.Principal.WindowsIdentity]::GetCurrent()
            $p  = New-Object Security.Principal.WindowsPrincipal($id)
            if (-not $p.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {
              Write-Host "[INFO] Elevation required. Re-launching as Administrator..."
              $psi = New-Object System.Diagnostics.ProcessStartInfo
              $psi.FileName  = "powershell.exe"
              $psi.Arguments = "-NoProfile -ExecutionPolicy Bypass -File `"$($MyInvocation.MyCommand.Path)`" " + ($args -join ' ')
              $psi.Verb = "runas"
              [Diagnostics.Process]::Start($psi) | Out-Null
              exit
            }
          }

          # WinForms GUI setup
          $Global:GuiReady = $false
          try {
            Add-Type -AssemblyName System.Windows.Forms -ErrorAction Stop
            $Global:GuiReady = $true
          } catch {
            $Global:GuiReady = $false 
            Write-Host "[WARN] Cannot load System.Windows.Forms. GUI dialogs will be disabled."
          }

          function Show-Info([string]$text) {
            if ($Global:GuiReady -and -not $Silent) {
              [void][System.Windows.Forms.MessageBox]::Show(
                $text,
                "ABLESTACK Installer",
                [System.Windows.Forms.MessageBoxButtons]::OK,
                [System.Windows.Forms.MessageBoxIcon]::Information
              )
            } else {
              Write-Host $text
            }
          }

          function Prompt_Reboot {
              param(
                  [string]$Message = "ABLESTACK tools installation is complete. The system needs to reboot.",
                  [string]$Title   = "ABLESTACK",
                  [int]   $Options = 0x0,     # 0x0: OK only / 0x4: Yes/No ??
                  [int]   $TimeoutSeconds = 60
              )

              $shell = New-Object -ComObject WScript.Shell

              # Popup return values:
              #   -1 = timeout
              #    1 = OK
              $result = $shell.Popup($Message, $TimeoutSeconds, $Title, $Options)

              if ($result -eq -1 -or $result -eq 1 -or $result -eq 6) {
                  Write-Host "Prompt_Reboot: user accepted or no response within timeout. Rebooting..."
                  # Force reboot after user confirmation or timeout.
                  Restart-Computer -Force
                  # MSI installs use /norestart and REBOOT=ReallySuppress; reboot is handled here.
              }
              else {
                  Write-Host "Prompt_Reboot: user canceled reboot (result = $result)."
              }
          }

          Ensure-Admin

          $logPath = "C:\Windows\Temp\ablestack-virtio-install.log"
          Start-Transcript -Path $logPath -Append | Out-Null

          $global:rebootNeeded = $false
          function Handle-MsiExit($code, $component, $logFile) {
            switch ($code) {
              0     { Write-Host "[OK] $component installed."; return }
              3010  { Write-Host "[OK] $component installed. Reboot required (3010)."
                      $global:rebootNeeded = $true; return }
              default { throw "$component install failed with code $code. See $logFile" }
            }
          }

          try {
            $ScriptDir = Split-Path -Path $MyInvocation.MyCommand.Path -Parent
            $VirtioDir = Join-Path $ScriptDir "virtio"
            $MsiDir    = Join-Path $ScriptDir "msi"

            if (-not (Test-Path $MsiDir)) { throw "MSI folder not found: $MsiDir" }

            # Locate VirtIO guest tools EXE
            $candidateExe = @(
              (Join-Path $ScriptDir  "virtio-win-guest-tools.exe"),
              (Join-Path $VirtioDir  "virtio-win-guest-tools.exe")
            ) + (Get-ChildItem -Path $VirtioDir -Recurse -Filter "virtio-win-guest-tools.exe" -ErrorAction SilentlyContinue | Select-Object -Expand FullName)
            $virtioExe = $candidateExe | Where-Object { Test-Path $_ } | Select-Object -First 1

            # MSI fallback
            $candidateMsi = @(
              (Join-Path $ScriptDir "virtio-win-gt-x64.msi"),
              (Join-Path $VirtioDir "virtio-win-gt-x64.msi")
            ) + (Get-ChildItem -Path $VirtioDir -Recurse -Filter "virtio-win-gt-*.msi" -ErrorAction SilentlyContinue | Select-Object -Expand FullName)
            $virtioMsi = $candidateMsi | Where-Object { Test-Path $_ } | Sort-Object -Descending | Select-Object -First 1

            $installOk = $false
            if ($virtioExe) {
              Write-Host "[STEP] Installing VirtIO guest tools (EXE): $virtioExe"
              $exeTry = @('/quiet /norestart','/qn /norestart','/S','/s')
              foreach ($argsStr in $exeTry) {
                Write-Host "  -> Trying args: $argsStr"
                $p = Start-Process -FilePath $virtioExe -ArgumentList $argsStr -PassThru -Wait -NoNewWindow
                if ($p.ExitCode -eq 0 -or $p.ExitCode -eq 3010) {
                  if ($p.ExitCode -eq 3010) { $global:rebootNeeded = $true }
                  $installOk = $true
                  Write-Host "[OK] VirtIO guest tools installed (EXE, code $($p.ExitCode))."
                  break
                } else {
                  Write-Warning "    ExitCode=$($p.ExitCode). Trying next switch..."
                }
              }
            }

            if (-not $installOk) {
              if (-not $virtioMsi) { throw "Neither virtio-win-guest-tools.exe nor virtio-win-gt-*.msi could be executed/found." }
              Write-Host "[STEP] Installing VirtIO guest tools (MSI fallback): $virtioMsi"
              $msiLog = Join-Path $env:TEMP "virtio-win-guest-tools-msi.log"
              $msiArgs = "/i `"$virtioMsi`" /qn /norestart /l*v `"$msiLog`" ADDLOCAL=ALL"
              $p = Start-Process -FilePath "msiexec.exe" -ArgumentList $msiArgs -PassThru -Wait -NoNewWindow
              Handle-MsiExit -code $p.ExitCode -component "VirtIO guest tools (MSI)" -logFile $msiLog
              $installOk = $true
            }

            # Install ablestack MSI
            Write-Host "[STEP] Installing ablestack-qemu-exec-tools (MSI)"
            $execMsi = Get-ChildItem -Path $MsiDir -Filter *.msi | Sort-Object LastWriteTime -Descending | Select-Object -First 1
            if (-not $execMsi) { throw "No ablestack MSI found in $MsiDir" }

            $execLog = Join-Path $env:TEMP "ablestack-qemu-exec-tools-msi.log"
            $execArgs = "/i `"$($execMsi.FullName)`" /qn /norestart /l*v `"$execLog`""
            $p = Start-Process -FilePath "msiexec.exe" -ArgumentList $execArgs -PassThru -Wait -NoNewWindow
            Handle-MsiExit -code $p.ExitCode -component "ablestack-qemu-exec-tools" -logFile $execLog

            # Completion message
            if ($global:rebootNeeded) {
              Prompt_Reboot "ABLESTACK tools installation completed. Reboot now?" "ABLESTACK" 0x0 30
            } else {
              Show-Info "Installation completed successfully. No reboot is required."
            }
          }
          catch {
            Write-Error $_
            exit 1
          }
          finally {
            Stop-Transcript | Out-Null
          }
          PS1

          cat > release/install.bat << 'BAT'
          @echo off
          setlocal
          rem Always run non-silent mode by default
          powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0install.ps1"
          if errorlevel 1 (
            echo [ERROR] Install failed. See C:\Windows\Temp\ablestack-virtio-install.log
            exit /b 1
          )
          
          REM --- Installation marker ---
          mkdir "C:\ProgramData\AbleStack" 2>nul
          echo ok> "C:\ProgramData\AbleStack\autoinstall.done"
          echo [OK] Installation finished.
          endlocal
          BAT

      - name: Create README.txt (updated)
        run: |
          cat <<'EOF' > release/README.txt
          ablestack-qemu-exec-tools Offline Installation ISO
          ==================================================
          Contents
            - RPM repos:
                rpm/rpm-rocky9.6
                rpm/ftctl-rpm-rocky9.6
            - DEB repos:
                deb/deb-ubuntu22.04
                deb/deb-ubuntu24.04
                deb/deb-ubuntu26.04
            - Windows:
                msi/ (ablestack-qemu-exec-tools .msi)
                virtio/ (VirtIO Windows drivers + Guest Tools from stable-virtio)
            - Universal installer:
                install-linux.sh
                uninstall-linux.sh
            - Shell completions:
                completions/ablestack_v2k
                completions/ablestack_vm_ftctl
                completions/ablestack_n2k
            - Windows installers:
                install.bat / install.ps1
            - FTCTL (ABLESTACK Host add-on):
                rpm/ftctl-rpm-rocky9.6
            - V2K (ABLESTACK Host add-on):
                v2k/v2k-rpm-rocky9.6 (offline RPM repo for ablestack_v2k)
                v2k/v2k-rpm-rocky9.7 (offline RPM repo for ablestack_v2k)
                v2k/v2k-rpm-rocky9.8 (offline RPM repo for ablestack_v2k)
                Runtime assets are bundled inside the ablestack_v2k RPM:
                  govc, VDDK, pyVmomi wheels, compatibility profiles, WinPE ISO
            - N2K (ABLESTACK Host add-on):
                n2k/n2k-rpm-rocky9.6 (offline RPM repo for ablestack_n2k)
                n2k/n2k-rpm-rocky9.7 (offline RPM repo for ablestack_n2k)
                n2k/n2k-rpm-rocky9.8 (offline RPM repo for ablestack_n2k)


          Windows (recommended)
            1) Mount ISO in the Windows VM.
            2) Run install.bat (or install.ps1) as Administrator.
               - Installs VirtIO Guest Tools (drivers & agent) silently.
               - Installs ablestack-qemu-exec-tools (MSI) silently.
            3) Reboot is recommended after completion.

          Linux
            1) mount -o loop ablestack-qemu-exec-tools-<VERSION>.iso /mnt
            2) cd /mnt && sudo ./install-linux.sh
            3) remove with sudo ./uninstall-linux.sh
          EOF

      - name: Create install-linux.sh (V2K add-on for ABLESTACK; keep existing flows)
        run: |
          cat <<'EOF' > release/install-linux.sh
          #!/usr/bin/env bash
          set -e
          ISO_MNT="$(cd "$(dirname "$0")" && pwd -P)"

          echo "[INFO] ABLESTACK QEMU Exec Tools Installer"
          echo "[INFO] Detecting distribution..."

          if [ -f /etc/os-release ]; then
              . /etc/os-release
              DISTRO=$ID
              MAJOR_VERSION=$(echo $VERSION_ID | cut -d'.' -f1)
          else
              echo "[ERROR] Cannot detect distribution!"
              exit 1
          fi

          resolve_v2k_repo_dir() {
              local repo_dir=
              if [[ -z "${VERSION_ID:-}" ]]; then
                  return 1
              fi
              repo_dir="${ISO_MNT}/v2k/v2k-rpm-rocky${VERSION_ID}"
              [[ -d "${repo_dir}" ]] || return 1
              printf '%s\n' "${repo_dir}"
          }

          resolve_n2k_rpm_repo_dir() {
              local repo_dir=
              if [[ -z "${VERSION_ID:-}" ]]; then
                  return 1
              fi
              repo_dir="${ISO_MNT}/n2k/n2k-rpm-rocky${VERSION_ID}"
              [[ -d "${repo_dir}" ]] || return 1
              printf '%s\n' "${repo_dir}"
          }

          has_cmd() { command -v "$1" >/dev/null 2>&1; }

          is_ablestack_host() {
              [[ "${PRETTY_NAME:-}" == *"ABLESTACK"* ]]
          }

          resolve_first_cmd() {
              local cmd=
              for cmd in "$@"; do
                  if has_cmd "${cmd}"; then
                      command -v "${cmd}"
                      return 0
                  fi
              done
              return 1
          }

          rpm_pkg_manager_cmd() {
              if is_ablestack_host && has_cmd aspm; then
                  command -v aspm
                  return 0
              fi
              resolve_first_cmd dnf aspm || {
                  echo "[ERROR] Neither dnf nor ABLESTACK aspm was found." >&2
                  return 127
              }
          }

          rpm_pkg_manager() {
              local cmd=
              cmd="$(rpm_pkg_manager_cmd)" || return $?
              sudo "${cmd}" "$@"
          }

          rpm_query_cmd() {
              if is_ablestack_host && has_cmd aspkg; then
                  command -v aspkg
                  return 0
              fi
              resolve_first_cmd rpm aspkg || {
                  echo "[ERROR] Neither rpm nor ABLESTACK aspkg was found." >&2
                  return 127
              }
          }

          rpm_query() {
              local cmd=
              cmd="$(rpm_query_cmd)" || return $?
              "${cmd}" "$@"
          }

          log_rpm_pkg_if_installed() {
              local pkg="${1-}"
              [[ -n "${pkg}" ]] || return 0
              rpm_query -q "${pkg}" || true
          }

          rpm_pkg_clean_all() {
              rpm_pkg_manager clean all || true
          }

          case "$DISTRO" in
              rocky|rhel|centos|almalinux|fedora)
                  echo "[INFO] Detected RPM-based system ($DISTRO $MAJOR_VERSION)"
                  REPO_FILE=/etc/yum.repos.d/ablestack-qemu-exec-tools.repo
                  REPO_PATH=""
                  for cand in "$ISO_MNT/rpm/rpm-rocky${VERSION_ID}" "$ISO_MNT/rpm/rpm-rocky${MAJOR_VERSION}" "$ISO_MNT/rpm/rpm-rocky9.6"; do
                      if [[ -d "${cand}" ]]; then
                          REPO_PATH="$(realpath "${cand}")"
                          break
                      fi
                  done
                  if [[ -z "${REPO_PATH}" ]]; then
                      echo "[ERROR] RPM repo not found in ISO for ${DISTRO} ${VERSION_ID}"
                      exit 1
                  fi
                  sudo tee "$REPO_FILE" >/dev/null <<EOR
          [ablestack-qemu-exec-tools]
          name=ablestack-qemu-exec-tools
          baseurl=file://$REPO_PATH
          enabled=1
          gpgcheck=0
          EOR
                  rpm_pkg_clean_all
                  rpm_pkg_manager -y --disablerepo="*" --enablerepo="ablestack-qemu-exec-tools" install ablestack-qemu-exec-tools
                  log_rpm_pkg_if_installed ablestack-qemu-exec-tools
                  sudo rm -f /etc/yum.repos.d/ablestack-qemu-exec-tools.repo
                  rpm_pkg_clean_all

                  # ======================================================================
                  # HANGCTL add-on (ABLESTACK Host only, Rocky 9.6 preferred)
                  #  - Install ablestack_vm_hangctl from ISO local repo
                  #  - If not ABLESTACK host / not Rocky9 -> skip
                  # ======================================================================
                  if [[ "${PRETTY_NAME:-}" == *"ABLESTACK"* && "${MAJOR_VERSION:-}" == "9" ]]; then
                      echo "[INFO] ABLESTACK Host on Rocky 9 detected. Installing HANGCTL add-on..."

                      HANGCTL_REPO_DIR=""
                      for cand in "${ISO_MNT}/rpm/hangctl-rpm-rocky${VERSION_ID}" "${ISO_MNT}/rpm/hangctl-rpm-rocky${MAJOR_VERSION}" "${ISO_MNT}/rpm/hangctl-rpm-rocky9.6"; do
                          if [[ -d "${cand}" ]]; then
                              HANGCTL_REPO_DIR="${cand}"
                              break
                          fi
                      done
                      if [[ -d "${HANGCTL_REPO_DIR}" ]]; then
                          HANGCTL_REPO_FILE=/etc/yum.repos.d/ablestack-vm-hangctl.repo
                          sudo tee "${HANGCTL_REPO_FILE}" >/dev/null <<-EOH
          [ablestack-vm-hangctl]
          name=ablestack-vm-hangctl
          baseurl=file://${HANGCTL_REPO_DIR}
          enabled=1
          gpgcheck=0
          EOH
                          rpm_pkg_clean_all
                          if rpm_pkg_manager -y --disablerepo="*" --enablerepo="ablestack-vm-hangctl" install ablestack_vm_hangctl; then
                              echo "[OK] ablestack_vm_hangctl installed."
                              log_rpm_pkg_if_installed ablestack_vm_hangctl
                          else
                              echo "[WARN] Failed to install ablestack_vm_hangctl from ISO repo. Continuing."
                          fi
                          sudo rm -f "${HANGCTL_REPO_FILE}"
                          rpm_pkg_clean_all
                      else
                          echo "[WARN] HANGCTL RPM repo not found in ISO: ${HANGCTL_REPO_DIR} (skip)"
                      fi
                  else
                      echo "[INFO] HANGCTL add-on skipped (not ABLESTACK host or not Rocky 9)."
                  fi

                  if [[ "${PRETTY_NAME:-}" == *"ABLESTACK"* && "${MAJOR_VERSION:-}" == "9" ]]; then
                      echo "[INFO] ABLESTACK Host on Rocky 9 detected. Installing FTCTL add-on..."

                      FTCTL_REPO_DIR=""
                      for cand in "${ISO_MNT}/rpm/ftctl-rpm-rocky${VERSION_ID}" "${ISO_MNT}/rpm/ftctl-rpm-rocky${MAJOR_VERSION}" "${ISO_MNT}/rpm/ftctl-rpm-rocky9.6"; do
                          if [[ -d "${cand}" ]]; then
                              FTCTL_REPO_DIR="${cand}"
                              break
                          fi
                      done
                      if [[ -d "${FTCTL_REPO_DIR}" ]]; then
                          FTCTL_REPO_FILE=/etc/yum.repos.d/ablestack-vm-ftctl.repo
                          sudo tee "${FTCTL_REPO_FILE}" >/dev/null <<-EOFCTL
          [ablestack-vm-ftctl]
          name=ablestack-vm-ftctl
          baseurl=file://${FTCTL_REPO_DIR}
          enabled=1
          gpgcheck=0
          EOFCTL
                          rpm_pkg_clean_all
                          if rpm_pkg_manager -y --disablerepo="*" --enablerepo="ablestack-vm-ftctl" install ablestack_vm_ftctl; then
                              echo "[OK] ablestack_vm_ftctl installed."
                              log_rpm_pkg_if_installed ablestack_vm_ftctl
                          else
                              echo "[WARN] Failed to install ablestack_vm_ftctl from ISO repo. Continuing."
                          fi
                          sudo rm -f "${FTCTL_REPO_FILE}"
                          rpm_pkg_clean_all
                      else
                          echo "[WARN] FTCTL RPM repo not found in ISO: ${FTCTL_REPO_DIR} (skip)"
                      fi
                  else
                      echo "[INFO] FTCTL add-on skipped (not ABLESTACK host or not Rocky 9)."
                  fi

                  # ======================================================================
                  # N2K add-on (ABLESTACK Host only)
                  #  - Nutanix AHV to ABLESTACK migration tool
                  # ======================================================================
                  if [[ "${PRETTY_NAME:-}" == *"ABLESTACK"* && "${MAJOR_VERSION:-}" == "9" ]]; then
                      if N2K_REPO_DIR="$(resolve_n2k_rpm_repo_dir)"; then
                          echo "[INFO] ABLESTACK ${VERSION_ID:-unknown} detected. Installing N2K add-on..."
                          N2K_REPO_FILE=/etc/yum.repos.d/ablestack-n2k.repo
                          sudo tee "${N2K_REPO_FILE}" >/dev/null <<-EON
          [ablestack-n2k]
          name=ablestack-n2k
          baseurl=file://${N2K_REPO_DIR}
          enabled=1
          gpgcheck=0
          EON
                          rpm_pkg_clean_all
                          if rpm_pkg_manager -y --disablerepo="*" --enablerepo="ablestack-n2k" install ablestack_n2k; then
                              echo "[OK] ablestack_n2k installed."
                              log_rpm_pkg_if_installed ablestack_n2k
                          else
                              echo "[WARN] Failed to install ablestack_n2k from ISO repo. Continuing."
                          fi
                          sudo rm -f "${N2K_REPO_FILE}"
                          rpm_pkg_clean_all
                      else
                          echo "[WARN] No bundled N2K repo matches host Rocky ${VERSION_ID:-unknown}."
                          echo "[WARN] Expected: ${ISO_MNT}/n2k/n2k-rpm-rocky${VERSION_ID:-<minor>}"
                      fi
                  else
                      echo "[INFO] N2K add-on skipped (not ABLESTACK host or not Rocky 9)."
                  fi

                  # ======================================================================
                  # V2K add-on (ABLESTACK Host only)
                  #  - based on bin/v2k_test_install.sh
                  #  - Air-Gap: pyvmomi installs from ISO offline wheels
                  #  - NO ldconfig / NO global linker path modifications
                  # ======================================================================
                  if [[ "${PRETTY_NAME:-}" == *"ABLESTACK"* && "${MAJOR_VERSION:-}" == "9" ]]; then
                      if V2K_REPO_DIR="$(resolve_v2k_repo_dir)"; then
                          echo "[INFO] ABLESTACK ${VERSION_ID:-unknown} detected. Installing V2K add-on..."

                          # 1) Install ablestack_v2k RPM from ISO local repo
                          V2K_REPO_FILE=/etc/yum.repos.d/ablestack-v2k.repo
                          sudo tee "${V2K_REPO_FILE}" >/dev/null <<-EOV
          [ablestack-v2k]
          name=ablestack-v2k
          baseurl=file://${V2K_REPO_DIR}
          enabled=1
          gpgcheck=0
          EOV
                          rpm_pkg_clean_all
                          rpm_pkg_manager -y --disablerepo="*" --enablerepo="ablestack-v2k" install ablestack_v2k || \
                            rpm_pkg_manager -y --disablerepo="*" --enablerepo="ablestack-v2k" install ablestack_v2k || \
                            rpm_pkg_manager -y localinstall "${V2K_REPO_DIR}"/*.rpm
                          log_rpm_pkg_if_installed ablestack_v2k
                          sudo rm -f "${V2K_REPO_FILE}"
                          rpm_pkg_clean_all

                          # 2) Validate RPM-installed compatibility profiles first.
                          #    RPM %post now installs govc, VDDK, pyVmomi wheels, and
                          #    profile definitions. Keep ISO asset install as a repair
                          #    fallback for older RPMs or manually damaged hosts.
                          V2K_INSTALLER_BIN="$(command -v v2k_test_install.sh || true)"
                          if [[ -z "${V2K_INSTALLER_BIN}" && -x "${ISO_MNT}/bin/v2k_test_install.sh" ]]; then
                              V2K_INSTALLER_BIN="${ISO_MNT}/bin/v2k_test_install.sh"
                          fi
                          if [[ -x "${V2K_INSTALLER_BIN}" ]]; then
                              echo "[INFO] Validating V2K compatibility profiles installed by RPM..."
                              if sudo "${V2K_INSTALLER_BIN}" --skip-install --validate-profile all; then
                                  echo "[OK] V2K compatibility profiles are already ready from RPM."
                              else
                                  echo "[WARN] RPM-installed V2K profiles are incomplete. Repairing from ISO assets..."
                                  sudo "${V2K_INSTALLER_BIN}" \
                                    --repo-root "${ISO_MNT}" \
                                    --skip-install \
                                    --install-assets \
                                    --install-profile all \
                                    --validate-profile all
                              fi
                          else
                              echo "[WARN] v2k_test_install.sh not found after ablestack_v2k installation."
                              echo "[WARN] Skipping compatibility profile asset installation."
                          fi

                          # WinPE is owned by the V2K RPM. Do not copy a second,
                          # independently selected ISO from the Tools media.
                          WINPE_DST_DIR="/usr/share/ablestack/v2k/winpe"
                          WINPE_LINK="/usr/share/ablestack/v2k/winpe.iso"
                          WINPE_META="${WINPE_DST_DIR}/current.json"
                          if [[ -f "${WINPE_META}" && -f "${WINPE_LINK}" ]]; then
                              WINPE_NAME="$(jq -r '.filename // empty' "${WINPE_META}")"
                              WINPE_EXPECTED="${WINPE_DST_DIR}/${WINPE_NAME}"
                              WINPE_CHECKSUM_OK=0
                              if (cd "${WINPE_DST_DIR}" && sha256sum -c SHA256SUMS); then
                                  WINPE_CHECKSUM_OK=1
                              fi
                              if [[ -z "${WINPE_NAME}" \
                                  || "$(readlink -e "${WINPE_LINK}" 2>/dev/null || true)" != "${WINPE_EXPECTED}" \
                                  || ! -f "${WINPE_EXPECTED}" \
                                  || "${WINPE_CHECKSUM_OK}" -ne 1 ]]; then
                                  echo "[ERR] RPM-installed WinPE ISO metadata/link/checksum validation failed." >&2
                                  exit 2
                              fi
                              echo "[OK] RPM-installed WinPE ISO verified: ${WINPE_EXPECTED}"
                          else
                              echo "[ERR] RPM-installed WinPE ISO metadata or compatibility link is missing." >&2
                              exit 2
                          fi
                      else
                          echo "[WARN] No bundled V2K repo matches host Rocky ${VERSION_ID:-unknown}."
                          echo "[WARN] Expected: ${ISO_MNT}/v2k/v2k-rpm-rocky${VERSION_ID:-<minor>}"
                      fi
                  else
                      echo "[INFO] V2K add-on skipped (not ABLESTACK host or not Rocky 9)."
                  fi
                  ;;
              ubuntu|debian)
                  echo "[INFO] Detected DEB-based system ($DISTRO $MAJOR_VERSION)"
                  UBUNTU_DIR="${MAJOR_VERSION}.04"
                  REPO_PATH=$(realpath "$ISO_MNT/deb/deb-ubuntu${UBUNTU_DIR}")
                  LIST_FILE=/etc/apt/sources.list.d/ablestack-qemu-exec-tools.list
                  echo "deb [trusted=yes] file://$REPO_PATH stable main" | sudo tee $LIST_FILE >/dev/null
                  sudo apt-get update
                  sudo apt-get -y install ablestack-qemu-exec-tools
                  sudo rm -f /etc/apt/sources.list.d/ablestack-qemu-exec-tools.list
                  sudo apt-get update
                  ;;
              *)
                  echo "[ERROR] Unsupported distribution: $DISTRO"
                  exit 1
                  ;;
          esac

          if [[ -d "${ISO_MNT}/completions" ]]; then
              echo "[INFO] Installing bash completion files from ISO..."
              sudo mkdir -p /usr/share/bash-completion/completions
              if [[ -f "${ISO_MNT}/completions/ablestack_v2k" ]]; then
                  sudo install -m 0644 "${ISO_MNT}/completions/ablestack_v2k" /usr/share/bash-completion/completions/ablestack_v2k
              fi
              if [[ -f "${ISO_MNT}/completions/ablestack_vm_ftctl" ]]; then
                  sudo install -m 0644 "${ISO_MNT}/completions/ablestack_vm_ftctl" /usr/share/bash-completion/completions/ablestack_vm_ftctl
              fi
              if [[ -f "${ISO_MNT}/completions/ablestack_n2k" ]]; then
                  sudo install -m 0644 "${ISO_MNT}/completions/ablestack_n2k" /usr/share/bash-completion/completions/ablestack_n2k
              fi
          fi

          # --- Installation marker ---
          mkdir -p /var/lib/ablestack
          echo ok > /var/lib/ablestack/autoinstall.done
          echo "[INFO] Installation complete."
          EOF
          chmod +x release/install-linux.sh

      - name: Sanity check generated install-linux.sh
        run: |
          set -e
          bash -n release/install-linux.sh
          grep -q 'is_ablestack_host && has_cmd aspm' release/install-linux.sh
          grep -q 'is_ablestack_host && has_cmd aspkg' release/install-linux.sh

      - name: Create uninstall-linux.sh
        run: |
          cat <<'EOF' > release/uninstall-linux.sh
          #!/usr/bin/env bash
          set -euo pipefail

          echo "[INFO] ABLESTACK QEMU Exec Tools Uninstaller"

          if [[ -f /etc/os-release ]]; then
            . /etc/os-release
            DISTRO="${ID:-unknown}"
            MAJOR_VERSION="$(echo "${VERSION_ID:-0}" | cut -d'.' -f1)"
          else
            echo "[ERROR] Cannot detect distribution!"
            exit 1
          fi

          remove_path_if_exists() {
            local path="${1-}"
            [[ -n "${path}" && -e "${path}" ]] || return 0
            sudo rm -rf "${path}"
          }

          has_cmd() { command -v "$1" >/dev/null 2>&1; }

          is_ablestack_host() {
            [[ "${PRETTY_NAME:-}" == *"ABLESTACK"* ]]
          }

          resolve_first_cmd() {
            local cmd=
            for cmd in "$@"; do
              if has_cmd "${cmd}"; then
                command -v "${cmd}"
                return 0
              fi
            done
            return 1
          }

          rpm_pkg_manager_cmd() {
            if is_ablestack_host && has_cmd aspm; then
              command -v aspm
              return 0
            fi
            resolve_first_cmd dnf aspm || {
              echo "[ERROR] Neither dnf nor ABLESTACK aspm was found." >&2
              return 127
            }
          }

          rpm_pkg_manager() {
            local cmd=
            cmd="$(rpm_pkg_manager_cmd)" || return $?
            sudo "${cmd}" "$@"
          }

          rpm_query_cmd() {
            if is_ablestack_host && has_cmd aspkg; then
              command -v aspkg
              return 0
            fi
            resolve_first_cmd rpm aspkg || {
              echo "[ERROR] Neither rpm nor ABLESTACK aspkg was found." >&2
              return 127
            }
          }

          rpm_query() {
            local cmd=
            cmd="$(rpm_query_cmd)" || return $?
            "${cmd}" "$@"
          }

          rpm_pkg_installed() {
            local pkg="${1-}"
            [[ -n "${pkg}" ]] || return 1
            rpm_query -q "${pkg}" >/dev/null 2>&1
          }

          remove_rpm_pkgs_if_installed() {
            local pkg=
            local pkgs_to_remove=()
            for pkg in "$@"; do
              if rpm_pkg_installed "${pkg}"; then
                pkgs_to_remove+=("${pkg}")
              else
                echo "[INFO] RPM package not installed, skip: ${pkg}"
              fi
            done
            if (( ${#pkgs_to_remove[@]} > 0 )); then
              rpm_pkg_manager -y remove "${pkgs_to_remove[@]}" || true
            fi
          }

          case "${DISTRO}" in
            rocky|rhel|centos|almalinux|fedora)
              echo "[INFO] Detected RPM-based system (${DISTRO} ${VERSION_ID:-unknown})"
              remove_rpm_pkgs_if_installed ablestack_v2k ablestack_n2k ablestack_vm_hangctl ablestack_vm_ftctl ablestack-qemu-exec-tools
              rpm_pkg_manager clean all || true
              ;;
            ubuntu|debian)
              echo "[INFO] Detected DEB-based system (${DISTRO} ${VERSION_ID:-unknown})"
              sudo apt-get -y remove ablestack-n2k ablestack-qemu-exec-tools || true
              sudo apt-get -y autoremove || true
              ;;
            *)
              echo "[ERROR] Unsupported distribution: ${DISTRO}"
              exit 1
              ;;
          esac

          remove_path_if_exists /etc/profile.d/v2k-vddk.sh
          remove_path_if_exists /etc/profile.d/v2k-compat.sh
          remove_path_if_exists /usr/share/ablestack/v2k
          remove_path_if_exists /usr/share/bash-completion/completions/ablestack_v2k
          remove_path_if_exists /usr/share/bash-completion/completions/ablestack_vm_ftctl
          remove_path_if_exists /usr/share/bash-completion/completions/ablestack_n2k

          if [[ -L /opt/vmware-vix-disklib-distrib ]]; then
            sudo rm -f /opt/vmware-vix-disklib-distrib
          fi
          if [[ -x /usr/local/bin/govc ]]; then
            sudo rm -f /usr/local/bin/govc
          fi

          if compgen -G "/opt/VMware-vix-disklib-*" >/dev/null 2>&1; then
            for d in /opt/VMware-vix-disklib-*; do
              [[ -e "${d}" ]] || continue
              sudo rm -rf "${d}"
            done
          fi

          sudo rm -f /var/lib/ablestack/autoinstall.done || true
          echo "[INFO] Uninstall complete."
          EOF
          chmod +x release/uninstall-linux.sh

      - name: Sanity check generated uninstall-linux.sh
        run: |
          set -e
          bash -n release/uninstall-linux.sh
          grep -q 'is_ablestack_host && has_cmd aspm' release/uninstall-linux.sh
          grep -q 'is_ablestack_host && has_cmd aspkg' release/uninstall-linux.sh

      - name: Generate release notes (changelog)
        run: |
          set -e

          CURRENT_TAG="${RELEASE_TAG}"
          CURRENT_REF="${RELEASE_REF:-${CURRENT_TAG}}"
          DESC="ABLESTACK QEMU Exec Tools is an automation toolkit for libvirt/KVM environments, providing guest command execution, VM lifecycle orchestration, and VMware-to-KVM (V2K) migration pipelines. This release includes ablestack_v2k with fully automated Windows cutover support using WinPE-based offline VirtIO driver injection."

          echo "# ablestack-qemu-exec-tools ${CURRENT_TAG}" > release_notes.md
          echo >> release_notes.md
          echo "${DESC}" >> release_notes.md
          echo >> release_notes.md

          # Build three OS-specific ISOs from staged release content
          # Each ISO contains only the payload relevant to that operating system.

          # Each ISO contains only the payload relevant to that operating system.
          PREV_TAG="$(git describe --tags --abbrev=0 "${CURRENT_REF}^" 2>/dev/null || true)"

          if [ -z "$PREV_TAG" ]; then
            echo "## Changes" >> release_notes.md
            git log --oneline "${CURRENT_REF}" -- . | sed 's/^/- /' >> release_notes.md
          else
            echo "## Changes since ${PREV_TAG}" >> release_notes.md
            git log --oneline "${PREV_TAG}..${CURRENT_REF}" -- . | sed 's/^/- /' >> release_notes.md
          fi

          echo "===== Generated release_notes.md ====="
          cat release_notes.md

      - name: Generate OS-specific ISOs
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${RELEASE_TAG}"

          mkdir -p build/iso/rocky build/iso/ubuntu build/iso/windows
          rm -rf build/iso-staging
          mkdir -p build/iso-staging/ubuntu build/iso-staging/windows

          cat <<'EOF' > build/iso-staging/ubuntu/README.txt
          ABLESTACK QEMU Exec Tools Offline ISO
          Target OS: Ubuntu

          Contents
            - deb/deb-ubuntu22.04
            - deb/deb-ubuntu24.04
            - deb/deb-ubuntu26.04
            - completions/
            - install-linux.sh
            - uninstall-linux.sh
          EOF

          cat <<'EOF' > build/iso-staging/windows/README.txt
          ABLESTACK QEMU Exec Tools Offline ISO
          Target OS: Windows

          Contents
            - msi/
            - virtio/
            - install.bat
            - install.ps1
          EOF

          for ver in 9.6 9.7 9.8; do
            rocky_staging="build/iso-staging/rocky${ver}"
            mkdir -p "${rocky_staging}"/{rpm,v2k,n2k,completions,bin}

            cat > "${rocky_staging}/README.txt" <<EOF
          ABLESTACK QEMU Exec Tools Offline ISO
          Target OS: Rocky Linux ${ver} / ABLESTACK Host

          Contents
            - rpm/rpm-rocky${ver}
            - rpm/hangctl-rpm-rocky${ver}
            - rpm/ftctl-rpm-rocky${ver}
            - v2k/v2k-rpm-rocky${ver}
            - n2k/n2k-rpm-rocky${ver}
            - completions/
            - install-linux.sh
            - uninstall-linux.sh
          EOF

            cp -a "release/rpm/rpm-rocky${ver}" "${rocky_staging}/rpm/"
            cp -a "release/rpm/hangctl-rpm-rocky${ver}" "${rocky_staging}/rpm/"
            cp -a "release/rpm/ftctl-rpm-rocky${ver}" "${rocky_staging}/rpm/"
            cp -a "release/v2k/v2k-rpm-rocky${ver}" "${rocky_staging}/v2k/"
            cp -a "release/n2k/n2k-rpm-rocky${ver}" "${rocky_staging}/n2k/"
            if [[ -d release/completions ]]; then
              cp -a release/completions/. "${rocky_staging}/completions/"
            fi
            if [[ -d release/bin ]]; then
              cp -a release/bin/. "${rocky_staging}/bin/"
            fi
            cp -a release/install-linux.sh "${rocky_staging}/"
            cp -a release/uninstall-linux.sh "${rocky_staging}/"

            volume_minor="${ver/.}"
            mkisofs -o "build/iso/rocky/ABLESTACK-Tools-rocky${ver}-${VERSION}.iso" \
              -V "ABLESTACK-Rocky${volume_minor}" \
              -r -J "${rocky_staging}"
          done

          mkdir -p build/iso-staging/ubuntu/deb build/iso-staging/ubuntu/completions
          cp -a release/deb/. build/iso-staging/ubuntu/deb/
          if [[ -d release/completions ]]; then
            cp -a release/completions/. build/iso-staging/ubuntu/completions/
          fi
          cp -a release/install-linux.sh build/iso-staging/ubuntu/
          cp -a release/uninstall-linux.sh build/iso-staging/ubuntu/

          mkdir -p build/iso-staging/windows/msi build/iso-staging/windows/virtio
          if [[ -d release/msi ]]; then
            cp -a release/msi/. build/iso-staging/windows/msi/
          fi
          if [[ -d release/virtio ]]; then
            cp -a release/virtio/. build/iso-staging/windows/virtio/
          fi
          cp -a release/install.bat build/iso-staging/windows/
          cp -a release/install.ps1 build/iso-staging/windows/

          mkisofs -o "build/iso/ubuntu/ABLESTACK-Tools-ubuntu-${VERSION}.iso" \
            -V "ABLESTACK-Ubuntu" \
            -r -J build/iso-staging/ubuntu

          mkisofs -o "build/iso/windows/ABLESTACK-Tools-windows-${VERSION}.iso" \
            -V "ABLESTACK-Windows" \
            -r -J build/iso-staging/windows

          find build/iso -type f -name '*.iso' -ls

      - name: Upload release build validation artifacts
        if: ${{ env.RELEASE_PUBLISH != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: release-validation-${{ github.run_number }}
          path: |
            build/iso/**
            release/rpm/rpm-rocky9.*/ablestack-qemu-exec-tools-*.rpm
            release/rpm/hangctl-rpm-rocky9.*/ablestack_vm_hangctl-*.rpm
            release/rpm/ftctl-rpm-rocky9.*/ablestack_vm_ftctl-*.rpm
            release/v2k/v2k-rpm-rocky9.6/ablestack_v2k-*.noarch.rpm
            release/v2k/v2k-rpm-rocky9.7/ablestack_v2k-*.noarch.rpm
            release/v2k/v2k-rpm-rocky9.8/ablestack_v2k-*.noarch.rpm
            release/n2k/n2k-rpm-rocky9.6/ablestack_n2k-*.noarch.rpm
            release/n2k/n2k-rpm-rocky9.7/ablestack_n2k-*.noarch.rpm
            release/n2k/n2k-rpm-rocky9.8/ablestack_n2k-*.noarch.rpm
            release/winpe/*.iso
            release/winpe/SHA256SUMS
            release_notes.md
          retention-days: 14

      - name: Stage uniquely named GitHub Release assets
        if: ${{ env.RELEASE_PUBLISH == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob

          OUT_DIR="release/upload-assets"
          rm -rf "${OUT_DIR}"
          mkdir -p "${OUT_DIR}"

          copy_plain() {
            local src="$1"
            local dst="${OUT_DIR}/$(basename "${src}")"
            cp -f "${src}" "${dst}"
          }

          copy_rpm_with_suffix() {
            local src="$1"
            local suffix="$2"
            local base name arch
            base="$(basename "${src}")"
            name="${base%.rpm}"
            arch="${name##*.}"
            if [[ "${arch}" == "${name}" ]]; then
              cp -f "${src}" "${OUT_DIR}/${name}-${suffix}.rpm"
            else
              cp -f "${src}" "${OUT_DIR}/${name%.*}-${suffix}.${arch}.rpm"
            fi
          }

          for iso in build/iso/rocky/*.iso build/iso/ubuntu/*.iso build/iso/windows/*.iso; do
            copy_plain "${iso}"
          done
          for winpe_asset in release/winpe/*.iso release/winpe/SHA256SUMS; do
            if [[ ! -f "${winpe_asset}" ]]; then
              echo "[ERR] Required WinPE release asset is missing: ${winpe_asset}" >&2
              exit 2
            fi
            copy_plain "${winpe_asset}"
          done

          for ver in 9.6 9.7 9.8; do
            for rpm in "release/rpm/rpm-rocky${ver}"/ablestack-qemu-exec-tools-*.rpm; do
              copy_rpm_with_suffix "${rpm}" "rocky${ver}"
            done
            for rpm in "release/rpm/hangctl-rpm-rocky${ver}"/ablestack_vm_hangctl-*.rpm; do
              copy_rpm_with_suffix "${rpm}" "rocky${ver}"
            done
            for rpm in "release/rpm/ftctl-rpm-rocky${ver}"/ablestack_vm_ftctl-*.rpm; do
              copy_rpm_with_suffix "${rpm}" "rocky${ver}"
            done
            for rpm in "release/v2k/v2k-rpm-rocky${ver}"/ablestack_v2k-*.noarch.rpm; do
              copy_rpm_with_suffix "${rpm}" "rocky${ver}"
            done
            for rpm in "release/n2k/n2k-rpm-rocky${ver}"/ablestack_n2k-*.noarch.rpm; do
              copy_rpm_with_suffix "${rpm}" "rocky${ver}"
            done
          done

          unexpected_rpm="$(find "${OUT_DIR}" -maxdepth 1 -type f -name '*.rpm' \
            ! -name 'ablestack-qemu-exec-tools-*.rpm' \
            ! -name 'ablestack_vm_hangctl-*.rpm' \
            ! -name 'ablestack_vm_ftctl-*.rpm' \
            ! -name 'ablestack_v2k-*.rpm' \
            ! -name 'ablestack_n2k-*.rpm' \
            -print -quit)"
          if [[ -n "${unexpected_rpm}" ]]; then
            echo "[ERR] Dependency RPM would be published: ${unexpected_rpm}" >&2
            exit 2
          fi

          asset_count="$(find "${OUT_DIR}" -maxdepth 1 -type f | wc -l)"
          if [[ "${asset_count}" -ne 22 ]]; then
            echo "[ERR] Expected exactly 22 GitHub Release assets, found ${asset_count}." >&2
            find "${OUT_DIR}" -maxdepth 1 -type f -printf '%f\n' | sort >&2
            exit 2
          fi

          oversized_asset="$(find "${OUT_DIR}" -maxdepth 1 -type f -size +2147483647c -print -quit)"
          if [[ -n "${oversized_asset}" ]]; then
            echo "[ERR] GitHub Release asset must be smaller than 2 GiB: ${oversized_asset}" >&2
            stat -c '[ERR] size=%s bytes file=%n' "${oversized_asset}" >&2
            exit 2
          fi

          echo "[INFO] GitHub Release assets to upload:"
          find "${OUT_DIR}" -maxdepth 1 -type f -printf '%f\n' | sort

      - name: Create GitHub Release
        if: ${{ env.RELEASE_PUBLISH == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ env.RELEASE_REF }}
          body_path: release_notes.md
          files: |
            release/upload-assets/*

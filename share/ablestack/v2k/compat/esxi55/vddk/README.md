Sample VDDK root for the `esxi55` compatibility profile.

Package or install a real ESXi 5.5-compatible VDDK distribution here. This
profile deliberately forbids falling back to the top-level VDDK asset because
newer VDDK releases can fail against ESXi 5.5 NFC data-plane sessions.
